// Exemplo DIO
const input = ["0", "0"];
let p = 0;
const gets = () => input[p++];

// Implementação desafio
let [A, B] = [parseInt(gets()), parseInt(gets())];

let SOMA = A + B; //Preencha os espaços em branco
console.log("SOMA = " + SOMA); //Preencha os espaços em branco
